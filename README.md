# Patched Versions Builder

A collection of scripts for making reproducible, patched builds of games from the Przygody Reksia series, shared for download on zagrajwreksia.pl.

`scripts/` directory contains a directory per each game. Each directory contains executable scripts which are run in order and applying changes to resulting zip file.